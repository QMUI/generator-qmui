Description:
  Creates a HTML file containing the basic code. The —include option will output template that uses QMUI Include Syntax rather than native HTML.

Example:
  yo qmui:html fileName [--include]

This will create:
  fileName.html
